import { OnlyNumber } from './only-number';

describe('OnlyNumber', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumber();
    expect(directive).toBeTruthy();
  });
});
